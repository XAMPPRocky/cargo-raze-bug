[package]
name = "a"
version = "0.1.0"
edition = "2021"
publish = ["cloudsmith"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ring = "0.16.20"
package = { version = "*", registry = "cloudsmith" }
